angular.module('app')
  .controller("FriendGroupController", ['$scope', '$http', '$rootScope',
    'requesthomestats',
    function($scope, $http, $rootScope, requesthomestats) {
      /*
      $http.defaults.headers.common = {
        'X-User': $rootScope.globals.currentUser.username,
        'X-Token': $rootScope.globals.currentUser.authdata
      };
      var initial = 0;
      $scope.searchFriendValue = "";
      var data_friends;
      url = 'https://envirocar.org/api/stable/users/';
      requesthomestats.get(url).then(function data {
        data_friends = data.data;
      });
      $scope.friends = {};
      $scope.searchFriends = function() {
        var helper_object = {};


      }
      */
    }
  ])
